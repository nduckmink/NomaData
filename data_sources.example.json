{
  "$comment": "Copy to data_sources.json (gitignored) and edit. Each entry is one connection. Use password_env instead of password to read a secret from an environment variable. Supported kinds: mysql (sqlserver next).",
  "sources": [
    {
      "name": "scp_mysql",
      "kind": "mysql",
      "host": "localhost",
      "port": 3307,
      "database": "stg_scp_app",
      "user": "scp",
      "password": "scp"
    }
  ]
}
