"""Core: provider-independent interfaces, shared models, errors, registry.

This package is the architectural center of NomaData. It depends on nothing
else inside ``nomadata`` — every other layer depends on it. See
docs/ARCHITECTURE.md.
"""
